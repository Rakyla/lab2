1. Начало.
2. Инициализировать генератор случайных чисел (srand(time(0))).
3. Для каждого из трёх файлов (file1.txt, file2.txt, file3.txt):
   а. Открыть файл для записи.
   б. Повторить 10 раз:
      - Сгенерировать случайное число от 1 до 10.
      - Записать число в файл.
      - Если это не последнее число — записать пробел.
   в. Перейти на новую строку.
   г. Закрыть файл.
4. Конец.
